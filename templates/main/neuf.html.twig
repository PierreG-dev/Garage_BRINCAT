{% extends 'base.html.twig' %}

{% block title %}SARL Garage BRINCAT{% endblock %}

{% block body %}

<style>
    body {
        background-color: #bdc3c7;
        background-size: cover;
    }
</style>


<div class="container-fluid p-5" style="min-height: 70vh;">
    <div class="row">
        {% for car in cars %}
        <div class="col-md-3 col-lg-3 col-sm-12 mb-5 text-center">
            <div class="car-card m-auto">
                <div class="top">
                    <img src="/images/properties/{{ car.filename }}" alt="{{car.filename}}">
                </div>
                <div class="bottom">
                    <h5 class="card-title">{{ car.brand }} {{ car.model }}</h5>
                    <p class="card-text">{{ car.Color }}<br>{{ car.price }} € HT<br>{{ car.energyType }}</p>
                </div>
            </div>
        </div>
        {% else %}
        RIEN A AFFICHER
        {% endfor %}
    </div>
</div>


{% endblock %}
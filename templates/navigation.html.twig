{% if app.user %}
<div class="overNav container-fluid" style="background-color: #009432;">
    {% else %}
    <div class="overNav container-fluid bg-dark">
        {% endif %}
        <div class="row" style="height: 100px;">
            <div class="col-5" style="margin:auto">
                <a class="navbar-home text-center text-white" href="{{ path('home') }}">
                    <div style="font-size: 2.1rem; line-height: 2.3rem; letter-spacing: 0.1em;">Garage <span
                            style="font-weight: bold">BRINCAT</span></div>
                </a>
            </div>
            <div class="col-7 text-white" style="margin: auto;">
                {% if not app.user %}
                <div class="row d-flex align-items-center">
                    <div class="col-lg-5 d-md-none d-lg-block"><i class="fa fa-envelope text-light"
                            aria-hidden="true"></i>
                        <a rel="noreferrer nofollow noopener" target="_blank" href="mailto:sarlgaragebrincat@orange.fr" class="text-light">sarlgaragebrincat@orange.fr</a>
                    </div>
                    <div class="col-lg-3 d-md-none d-lg-block"><i class="fa fa-phone text-light" aria-hidden="true"></i>
                        <a rel="noreferrer nofollow noopener" target="_blank" href="tel:+05 63 32 33 78" class="text-light">05 63 32 33 78</a>
                    </div>
                    <div class="col-lg-4 col-md-9 d-flex align-items-center">
                        <i class="text-light" style="font-weight: 600">Protégeons nous les uns les autres</i>
                        <img src="{{ asset('res/garage-castelsarrasin-montauban-masque-obligatoire.png') }}" alt="garage castelsarrasin montauban port du masque obligatoire"
                            style="width: 70px;height: auto;margin-left: 10px;">
                    </div>
                </div>
                {% else %}
                <p style="text-align: center; color: #efefef; text-transform: capitalize;font-size: 1.9rem;">
                    Bienvenue <span style="font-weight: bold;">{{ app.user.username }}</span>
                </p>
                {% endif %}
            </div>
        </div>
    </div>

    <div class="navbar navbar-expand-lg navbar-dark"
        style="background: #343537; position: sticky !important; top: 0; z-index: 2">
        <a class="navbar-brand" href="{{ path('home') }}" style:"opacity:0;">Sarl Garage Brincat</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav" style="margin-left: 10vw;">
            <ul class="navbar-nav">
                <li class="nav-item"
                    style="padding-right: 0.5rem;padding-left: 0.5rem; border-right: 1px solid rgba(255, 255, 255, 0.2); display: flex; align-items: center; line-height: 10px">
                    <a class="nav-link" href="{{ path('home') }}">Accueil <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item"
                    style="padding-right: 0.5rem;padding-left: 0.5rem; border-right: 1px solid rgba(255, 255, 255, 0.2); display: flex; align-items: center; line-height: 10px">
                    <a class="nav-link" href="{{ path('neuf') }}">Offres neuf</a>
                </li>
                <li class="nav-item"
                    style="padding-right: 0.5rem;padding-left: 0.5rem; border-right: 1px solid rgba(255, 255, 255, 0.2); display: flex; align-items: center; line-height: 10px">
                    <a class="nav-link" href="{{ path('occasion') }}">Offres occasions</a>
                </li>
                <li class="nav-item"
                    style="padding-right: 0.5rem;padding-left: 0.5rem; border-right: 1px solid rgba(255, 255, 255, 0.2); display: flex; align-items: center; line-height: 10px">
                    <a class="nav-link" href="{{ path('contact') }}">Contact</a>
                </li>
                <li class="nav-item"
                    style="padding-right: 0.5rem;padding-left: 0.5rem; display: flex; align-items: center; line-height: 10px">
                    <a class="nav-link" href="{{ path('about') }}">A propos</a>
                </li>
            </ul>
            <ul class="navbar-nav flex-row ml-md-auto d-none d-md-flex">
                {% if app.user %}
                <li>
                    <abbr title="Administration">
                        <a class="nav-link" href="{{ path('admin') }}">
                            <i class="fa fa-wrench" aria-hidden="true"></i>
                        </a>
                    </abbr>
                </li>
                {% endif %}
                <li class="nav-item mr-md-2">
                    {% if not app.user %}
                    <abbr title="Se connecter">
                        <a class="nav-link" href="{{ path('authentification') }}">
                            <i class="fa fa-lock" aria-hidden="true"></i>
                        </a>
                    </abbr>
                    {% else %}
                    <abbr title="Se déconnecter">
                        <a class="nav-link" href="{{ path('admin_logout') }}">
                            <i class="fa fa-sign-out" aria-hidden="true"></i>
                        </a>
                    </abbr>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
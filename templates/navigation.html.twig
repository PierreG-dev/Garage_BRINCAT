{% if app.user %}
<div class="overNav container-fluid" style="background-color: #009432;">
{% else %}
<div class="overNav container-fluid bg-primary">
{% endif %}
    <div class="row" style="height: 10vh;">
        <div class="col-5" style="margin:auto">
            <a class="navbar-home text-center" href="{{ path('home') }}"x"><h1 style="font-size: 2.1rem;">Garage <span style="font-weight: bold">BRINCAT</span></h1></a>
        </div>
        <div class="col-7 text-white" style="margin: auto;">
            {% if not app.user %}
                <div class="row">
                    <div class="col-4">sarlgaragebrincat@orange.fr</div>
                    <div class="col-4">Mon–Sat: 7:00–19:00</div>
                    <div class="col-4">05 63 32 33 78</div>
                </div>
            {% else %}
                <h2 style="text-align: center; color: #efefef; text-transform: capitalize;">Bienvenue <span style="font-weight: bold;">{{ app.user.username }}</span></h2>
            {% endif %}
            
        </div>
    </div>
</div>

<div class="navbar navbar-expand-lg navbar-dark" style="background: #343537;">
    <a class="navbar-brand" href="#"></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav" style="margin-left: 10vw;">
        <ul class="navbar-nav">
            <li class="nav-item" style="padding-right: 0.5rem;padding-left: 0.5rem; border-right: 1px solid rgba(255, 255, 255, 0.2); display: flex; align-items: center; line-height: 10px">
                <a class="nav-link" href="{{ path('home') }}">Accueil <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item" style="padding-right: 0.5rem;padding-left: 0.5rem; border-right: 1px solid rgba(255, 255, 255, 0.2); display: flex; align-items: center; line-height: 10px">
                <a class="nav-link" href="{{ path('neuf') }}">Neuf</a>
            </li>
            <li class="nav-item" style="padding-right: 0.5rem;padding-left: 0.5rem; border-right: 1px solid rgba(255, 255, 255, 0.2); display: flex; align-items: center; line-height: 10px">
                <a class="nav-link" href="{{ path('occasion') }}">Occasions</a>
            </li>
            <li class="nav-item" style="padding-right: 0.5rem;padding-left: 0.5rem; border-right: 1px solid rgba(255, 255, 255, 0.2); display: flex; align-items: center; line-height: 10px">
                <a class="nav-link" href="{{ path('about') }}">Contact</a>
            </li>
            <li class="nav-item" style="padding-right: 0.5rem;padding-left: 0.5rem; display: flex; align-items: center; line-height: 10px">
                <a class="nav-link" href="{{ path('contact') }}">A propos</a>
            </li>
        </ul>
        <ul class="navbar-nav flex-row ml-md-auto d-none d-md-flex">
            {% if app.user %}
            <li>
                <abbr title="Administration"><a class="nav-link" href="{{ path('admin') }}"><i class="fa fa-wrench" aria-hidden="true"></i></a></abbr>
            </li>
            {% endif %}
            <li class="nav-item mr-md-2">
                {% if not app.user %}
                    <abbr title="Se connecter"><a class="nav-link" href="{{ path('authentification') }}"><i class="fa fa-lock" aria-hidden="true"></i></a></abbr>
                {% else %}
                    <abbr title="Se déconnecter"><a class="nav-link" href="{{ path('admin_logout') }}"><i class="fa fa-sign-out" aria-hidden="true"></i></a></abbr>
                {% endif %}
            </li>
            
            
        </ul>
            
        
    </div>
</div>
{% extends 'base_admin.html.twig' %}

{% block title %}Ajouter une voiture{% endblock %}

{% block body %}

<style>
    body {
        background: url("{{ asset('res/admin_body_dark.svg') }}");
        background-size: cover;
    }
</style>

<h1 class="text-center text-white">Ajouter une voiture</h1>

    {{ include('car/_form.html.twig') }}

    <div class="container m-auto">
        <a href="{{ path('car_index') }}" class="btn btn-info mt-2">Retourner à la liste</a>
    </div>
{% endblock %}

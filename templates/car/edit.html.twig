{% extends 'base_admin.html.twig' %}

{% block title %}Modification voiture{% endblock %}

{% block body %}
    <h1>Modifier la voiture</h1>

    {{ include('car/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="row">
        <div class="col-2"></div>
        <div class="col-8">
            <a href="{{ path('car_index') }}">Retourner à la liste</a>
            {{ include('car/_delete_form.html.twig') }}
        </div>
        <div class="col-2"></div>
        
    </div>

    
{% endblock %}
